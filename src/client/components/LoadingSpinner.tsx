import React from 'react';

const LoadingSpinner: React.FC = () => {
  return <div className="spinner-border animate-spin inline-block w-8 h-8 border-4 rounded-full"></div>;
};

export default LoadingSpinner;
